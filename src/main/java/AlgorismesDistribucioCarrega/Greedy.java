package AlgorismesDistribucioCarrega;

import JSONClasses.Server;
import JSONClasses.User;
import AlgorismesDistribucioCarrega.Solution;

import java.util.ArrayList;
import java.util.List;

import static java.sql.JDBCType.NULL;

public class Greedy {

    public List<Solution> greedyDistribucioCarrega(Server[] servers, User[] users){
        List<Solution> candidates = new ArrayList<Solution>();
        List<Solution> s = new ArrayList<Solution>();

        candidates= ;

        while (candidates.size() > 0) {
            c = ;

            if (){
                s.add(,);
                s.get(i).sumarCarrega();
            }
        }

        //Considerem que el resultat sempre és correcte, ja que aquest algorismes només s'encarrega de distribuir
        //tots els usuaris entre els diferents servidors, per tant la solucó sempre correcte

        return s;
    }

    public Solution select(){

        return candidates;
    }

    public Solution is_feaseble(){

        return ;
    }

    public Solution is_Solution(){

        return s;
    }

}
